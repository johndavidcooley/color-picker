import React from 'react';
import styled from 'styled-components';

const RightSwatch = ({className}) => {
  return (
    <div className={`${className} right-swatch`}>

    </div>
  )
};

export default styled(RightSwatch)`
  background-color: dodgerblue;
`;